{
    "name": "AstroInsight",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": "wasp start",
        "build": "wasp build",
        "start": "wasp start prod",
        "db:migrate:dev": "wasp db migrate-dev",
        "db:deploy": "wasp db migrate-deploy",
        "build:prod": "wasp build"
    },
    "devDependencies": {
        "wasp-cli": "^0.13.0"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}
